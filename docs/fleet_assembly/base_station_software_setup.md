# Base Station Software Setup

- Install the project dependencies.

  ```bash
  sudo apt-get update && sudo apt-get install -y git openssh-client mosh rsync avahi-daemon libnss-mdns
  ```

- Complete the [Get Started](https://github.com/cougars-auv/cougars-dev/blob/main/README.md#get-started) guide in `README.md`.
